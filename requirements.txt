# Core dependencies for TG WS Proxy
# Works on Windows, Linux, and macOS

cryptography>=46.0.5

# Tray and GUI (cross-platform)
customtkinter>=5.2.2
Pillow>=12.1.1
psutil>=7.2.2
pystray>=0.19.5

# Windows-specific
pyperclip>=1.11.0; sys_platform == "win32"

# macOS-specific (native menu bar)
rumps>=0.4.0; sys_platform == "darwin"

# QR code generation
qrcode>=8.2

# Web dashboard (optional)
flask>=3.1.3
flask-cors>=6.0.2

# Console dashboard (optional, for better TUI)
rich>=14.3.3
markdown-it-py>=3.0.0

# Async DNS resolver (optional, for better performance)
aiodns>=3.2.0; sys_platform != "win32"

# Cross-platform directories
appdirs>=1.4.4

# Linux-specific
# pylibappindicator; sys_platform == "linux"  # Optional, for AppIndicator support
