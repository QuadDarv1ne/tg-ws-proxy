# TODO — TG WS Proxy

## Текущий статус
- ✅ Базовая функциональность SOCKS5-прокси
- ✅ WebSocket relay к Telegram DC
- ✅ TCP fallback при недоступности WS
- ✅ Tray-приложение для Windows
- ✅ GUI настройки
- ✅ Кроссплатформенная поддержка (Linux, macOS)
- ✅ Вынос констант в `constants.py`
- ✅ Type hints для публичного API
- ✅ RotatingFileHandler для логов (5MB, 3 backup)
- ✅ Unit-тесты для критической логики
- ✅ Статистика в трее
- ✅ Автозапуск (Windows, macOS, Linux)
- ✅ Проверка обновлений
- ✅ CI/CD пайплайн (GitHub Actions)
- ⚠️ Требуется расширение тестов и рефакторинг

---

## 🐛 Критические проблемы

### 1. IPv6 Warning показывается только один раз
**Файл:** `windows.py`
**Статус:** ✅ Частично решено — предупреждение показывается один раз за сессию (маркер `.ipv6_warned`).
**Остаточная проблема:** Если пользователь не отключил IPv6 после первого предупреждения, проблема остаётся.
**Решение:** Добавить проверку IPv6 в рантайме и логирование предупреждений при проблемах подключения.

### 2. Отсутствие обработки ошибок при bind на порт
**Файл:** `windows.py`, `_run_proxy_thread()`
**Статус:** ✅ Решено — добавлена функция `_check_port_available()` с предварительной проверкой.

---

## 🔧 Улучшения архитектуры

### 1. Вынести константы в отдельный модуль
**Файлы:** `proxy/tg_ws_proxy.py`
**Статус:** ✅ Решено — создан `proxy/constants.py` с константами.

### 2. Типизация
**Файлы:** Все `.py` файлы
**Статус:** ✅ Частично решено — добавлены type hints для публичного API и `ProxyConfig` TypedDict.
**Остаточная задача:** Добавить type hints для всех внутренних функций.

### 3. Логирование
**Файлы:** `proxy/tg_ws_proxy.py`, `windows.py`
**Статус:** ✅ Частично решено — добавлен RotatingFileHandler (5MB, 3 backup).
**Остаточная задача:** JSON format для файлов, уровни логирования через конфиг.

---

## 📦 Зависимости

### 1. Разделить зависимости по окружениям
**Файлы:** `requirements.txt`, `requirements-win7.txt`
**Статус:** ✅ Решено — есть `requirements-dev.txt`, `requirements-build.txt`.
**Проблема:** Нет чёткого разделения на runtime и dev зависимости в `requirements.txt`.
**Решение:** Оставить в `requirements.txt` только базовые зависимости, dev — в `requirements-dev.txt`.

### 2. Добавить pyproject.toml
**Статус:** ✅ Решено — `pyproject.toml` создан с зависимостями, метаданными и конфигурацией линтеров.

---

## 🧪 Тестирование

### 1. Unit-тесты
**Статус:** ✅ Частично решено — есть `tests/test_proxy.py` с тестами для:
- `_dc_from_init()` — извлечение DC ID
- `_patch_init_dc()` — патчинг init-пакета
- `_MsgSplitter.split()` — разбиение сообщений
- `_is_telegram_ip()` — проверка IP-диапазонов
- `parse_dc_ip_list()` — парсинг конфигов
- `_is_http_transport()` — определение HTTP-трафика

**Остаточная задача:** Добавить тесты для `RawWebSocket`, `_WsPool`, `Stats`.

### 2. Интеграционные тесты
**Статус:** ❌ Не начато.
**Задача:** Тесты на реальное подключение (с моком WebSocket сервера):
- SOCKS5 handshake
- WebSocket подключение к тестовому серверу
- TCP fallback сценарий

### 3. CI/CD
**Файл:** `.github/workflows/build.yml`
**Статус:** ✅ Решено — GitHub Actions workflow.
**Реализация:**
- Сборка для Windows (обычная + Win7)
- PyInstaller упаковка
- Автоматические релизы при запуске workflow
- Кэширование pip зависимостей

---

## 🎨 UI/UX улучшения

### 1. Статистика в реальном времени
**Файл:** `windows.py`
**Статус:** ✅ Решено — добавлен диалог статистики с отображением:
- Текущее количество подключений
- Статус по типам (WS/TCP fallback/HTTP rejected)
- Трафик upload/download
- Pool hits/misses

### 2. Автозапуск
**Статус:** ✅ Решено — добавлена функция `_toggle_autostart_dialog()` для Windows/macOS/Linux.
**Реализация:**
- Windows: реестр `HKCU\Software\Microsoft\Windows\CurrentVersion\Run`
- macOS: LaunchAgent plist-файл
- Linux: `.desktop` файл в XDG autostart

### 3. Проверка обновлений
**Статус:** ✅ Решено — добавлена функция `_check_for_updates()`.
**Реализация:**
- Проверка раз в 24 часа через GitHub API
- Уведомление с кнопкой открытия релиза
- Пропуск pre-release версий
- Версионирование через `CURRENT_VERSION = "1.3.0"`

---

## 📝 Документация

### 1. CONTRIBUTING.md
**Статус:** ❌ Не начато.
**Задача:** Гайд для контрибьюторов:
- Как запустить dev-окружение
- Как запускать тесты
- Style guide

### 2. CHANGELOG.md
**Статус:** ❌ Не начато.
**Задача:** Вести changelog по принципу Keep a Changelog.

### 3. Примеры использования
**Статус:** ❌ Не начато.
**Задача:** Добавить примеры:
- Запуск через Docker
- Настройка на Linux/macOS
- Использование с другими клиентами (не только Telegram Desktop)

---

## 🔒 Безопасность

### 1. Валидация входных данных
**Файлы:** `windows.py`, `_edit_config_dialog()`
**Статус:** ✅ Частично решено — есть валидация IP, порта, DC маппингов.
**Остаточная проблема:** Недостаточная валидация пользовательского ввода.
**Решение:** Строгая валидация всех полей конфига.

### 2. Secrets management
**Статус:** ❌ Не требуется на данный момент.
**Задача:** Если в будущем добавится поддержка авторизации — не хранить пароли в plaintext.

---

## 🚀 Производительность

### 1. Tuning параметров
**Файл:** `proxy/constants.py`
**Статус:** ✅ Решено — все параметры вынесены в константы:
- `WS_POOL_SIZE` — размер пула WebSocket соединений
- `WS_POOL_MAX_AGE` — время жизни соединения в пуле
- `RECV_BUF_SIZE`, `SEND_BUF_SIZE` — размеры буферов
- `DC_FAIL_COOLDOWN` — кулдаун после ошибок

**Остаточная задача:** Сделать параметры настраиваемыми через конфиг.

### 2. Профилирование
**Статус:** ❌ Не начато.
**Задача:** Добавить профилирование для узких мест:
- Время подключения к WS
- Время обработки сообщений
- Использование памяти

---

## 📊 Метрики и мониторинг

### 1. Prometheus metrics endpoint
**Статус:** ❌ Не начато.
**Задача:** Опциональный HTTP endpoint для экспорта метрик:
- Количество активных подключений
- Статус по DC (WS/TCP)
- Байты отправлено/получено
- Ошибки по типам

### 2. Health check
**Статус:** ❌ Не начато.
**Задача:** Endpoint для проверки здоровья прокси.

---

## 🧹 Технический долг

### 1. Глобальные переменные
**Файлы:** `proxy/tg_ws_proxy.py`
**Статус:** ⚠️ Требует рефакторинга.
**Проблема:** `_dc_opt`, `_ws_blacklist`, `_stats`, `_ws_pool` — глобальное состояние.
**Решение:** Инкапсулировать в класс `ProxyServer`.

### 2. Магические числа
**Файлы:** `proxy/tg_ws_proxy.py`, `proxy/constants.py`
**Статус:** ✅ Решено — все числа вынесены в именованные константы с комментариями.

### 3. Обработка ошибок
**Файлы:** Все файлы
**Статус:** ⚠️ Частично решено.
**Проблема:** Много `except Exception` без спецификации.
**Решение:** Ловить конкретные исключения, логировать стектрейс в debug режиме.

---

## 📋 Приоритеты

### Высокий приоритет
1. ✅ IPv6 warning в рантайме
2. ✅ Предварительная проверка порта
3. ⚠️ Unit-тесты для критической логики (частично)
4. ✅ Вынос констант в constants.py
5. ✅ Магические числа в именованные константы
6. ✅ RotatingFileHandler для логов
7. ⚠️ Type hints для публичного API (частично)
8. ✅ UI статистики в трее
9. ✅ Кроссплатформенная поддержка (Windows, Linux, macOS)
10. ✅ CI/CD пайплайн
11. ✅ Автозапуск
12. ✅ Проверка обновлений

### Средний приоритет
1. ❌ CONTRIBUTING.md
2. ❌ Расширение unit-тестов (RawWebSocket, WsPool, Stats)
3. ⚠️ Рефакторинг глобального состояния
4. ❌ Интеграционные тесты

### Низкий приоритет
1. ❌ Prometheus metrics
2. ❌ Docker поддержка
3. ❌ Профилирование производительности
4. ❌ JSON формат логирования

---

## 📈 Roadmap

### v1.1.0 (Bug fixes) ✅ Released
- ✅ Исправление IPv6 warning
- ✅ Проверка порта перед запуском
- ✅ Базовые unit-тесты

### v1.2.0 (DX improvements) ✅ Released
- ✅ Type hints для публичного API
- ✅ Разделение зависимостей
- ✅ pyproject.toml
- ✅ CI/CD пайплайн

### v1.3.0 (Features) ✅ In progress
- ✅ Статистика в трее
- ✅ Кроссплатформенность (Windows, Linux, macOS)
- ✅ Автозапуск
- ✅ Проверка обновлений
- ⚠️ Расширение unit-тестов

### v2.0.0 (Architecture)
- ❌ Рефакторинг глобального состояния
- ❌ Docker образ
- ❌ Интеграционные тесты
